<div class="text-center">
  <%= form_with model: @board, local: true do |f| %>
    <%= f.label :title, "ボード名" %>
    <%= f.text_field :title %>
    <%= f.submit "作成", class: 'btn' %>
  <% end %>
</div>
